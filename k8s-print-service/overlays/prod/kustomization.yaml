apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: prod-print-service

resources:
  - ../../base

patches:
  - patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: examx-print-service
        labels:
          app: examx-print-service
          environment: production
        annotations:
          kubernetes.io/change-cause: "Deployed to production environment"
      spec:
        replicas: 3
        selector:
          matchLabels:
            app: examx-print-service
        template:
          metadata:
            labels:
              app: examx-print-service
              environment: production
          spec:
            containers:
              - name: examx-print-service
                image: "399600302704.dkr.ecr.ap-south-1.amazonaws.com/examx-print-service:prod-latest"
                imagePullPolicy: Always
                resources:
                  requests:
                    cpu: "100m"
                    memory: "512Mi"
                  limits:
                    cpu: "500m"
                    memory: "1Gi"
                env:
                  - name: NODE_ENV
                    value: "production"
                  - name: PORT
                    value: "3000"
    target:
      kind: Deployment
      name: examx-print-service
  
  - patch: |-
      apiVersion: autoscaling/v2
      kind: HorizontalPodAutoscaler
      metadata:
        name: examx-print-service-autoscaler
      spec:
        minReplicas: 3
        maxReplicas: 10
    target:
      kind: HorizontalPodAutoscaler
      name: examx-print-service-autoscaler
